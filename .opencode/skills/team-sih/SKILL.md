# Team SIH (DepthWizard)

- Trace every task to SIH rows R1–R15 in
  `docs/project/SIH_REQUIREMENT_TRACEABILITY.md`.
- Cover both input paths (A: PNG/JPG relative; B: GeoTIFF metric when
  justified) and the full chain to flythrough + validation +
  standalone deployment.
- Planning alone never completes a requirement — only evidenced
  verification does.
- End state: the Final Acceptance Gate checklist fully evidenced.
