# Team Release (DepthWizard)

- Gates 1–11 in `docs/project/RELEASE_GATES.md`; a gate passes only
  with recorded evidence for every prerequisite.
- Release-gate items carry `Release Gate = Yes` in the Project.
- Standalone bar: fresh-machine install → launch → full pipeline with
  no dev-path dependency, install docs, license/provenance bundle.
- Final merge authority: Shivam. GATE 11 closes only when the full
  acceptance checklist is evidenced.
