# Team Scientific (DepthWizard)

- Relative depth ≠ metric DSM. Metric output requires an explicit
  calibration against validated DEM/GCP/reference controls, with
  method + units + provenance + validity recorded.
- Verification ladder: unit → integration → scientific → runtime →
  visual → end-to-end. No Done without the issue's verification type
  evidenced.
- Current GAMUS evidence (MAE 4.40 m / RMSE 5.86 m / R² 0.23,
  32-tile pooled) is a research signal, not SIH validation.
- Research → product promotion follows
  `docs/project/RESEARCH_VS_PRODUCT.md`. Never relabel research
  numbers as product claims.
