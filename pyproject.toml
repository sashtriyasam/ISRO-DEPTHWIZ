[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "depthwizard"
version = "1.0.0"
description = "DepthWizard scientific engine — single-view height estimation contracts (foundation only, no inference)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0,<3",
    "Pillow>=11,<13",
    "rasterio>=1.4,<2",
    # Direct runtime since S11: DSMGrid array storage uses NumPy in
    # production code (previously dev-only + transitive via rasterio).
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "mypy>=1",
    "ruff>=0.4",
]
dav2 = [
    "torch>=2.0",
    "torchvision>=0.15",
    "opencv-python>=4.5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# UP042 (str,Enum -> StrEnum) ignored: the explicit mixin keeps
# identical runtime semantics on every 3.11+ interpreter.
ignore = ["UP042"]

[tool.mypy]
python_version = "3.11"
strict = true
show_error_codes = true

[[tool.mypy.overrides]]
module = ["numpy", "numpy.*"]
ignore_errors = true

# rasterio ships no stubs/py.typed (verified with rasterio 1.5.1):
# skip type analysis of its imports instead of failing the run.
[[tool.mypy.overrides]]
module = ["rasterio", "rasterio.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["torch", "torch.*", "torchvision", "torchvision.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["cv2", "cv2.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["depth_anything_v2", "depth_anything_v2.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["PIL", "PIL.*"]
ignore_missing_imports = true

# h5py ships no py.typed marker: skip type analysis of its imports.
# It is a data-preparation-only dependency (GAMUS tile reading).
[[tool.mypy.overrides]]
module = ["h5py", "h5py.*"]
ignore_missing_imports = true
