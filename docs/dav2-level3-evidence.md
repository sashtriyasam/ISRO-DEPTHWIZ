# DA-V2 Level-3 System Evidence

Machine-readable record: `docs/dav2-level3-evidence.json` (generated by
`scripts/dav2_level3_evidence.py`; timings below are single-session CPU
engineering observations, NOT benchmarks).

## Baseline

| Item               | Value                                                                                    |
| ------------------ | ---------------------------------------------------------------------------------------- |
| Repository SHA     | `07bc635f7ace61370ff9a6d1f9ea778426a8dda7` (main)                                        |
| Model              | Depth Anything V2 Small, encoder vits, 24,785,089 params                                 |
| Checkpoint         | `depth_anything_v2_vits.pth` (external, git-ignored)                                     |
| Checkpoint SHA-256 | `715fade13be8f229f8a70cc02066f656f2423a59effd0579197bbf57860e1378`                       |
| Upstream           | `DepthAnything/Depth-Anything-V2` @ `a561b849...`                                        |
| Environment        | Windows, Python 3.12.10, torch 2.14.0+cpu, torchvision 0.29.0+cpu, cv2 5.0.0, device cpu |

`upstream_revision` (git commit) and `checkpoint_sha256` (file hash) are
separate provenance fields. The file hash was recomputed from the local
checkpoint during this task and matches the code constant.

## Achieved (prior levels, reused as evidence)

Level 1 — adapter correctness. Level 2 — real DA-V2 runtime verification
(relative output, finite values, source-size restoration, deterministic
repeat, LocalService selection, PipelineRunner COMPLETED, DSM/mesh smoke,
transport preservation).

## Verified in Level 3 (this task)

### A. Representative-image engineering observations

Deterministic fixtures: seed 26175, gradient + sinusoid + rectangle +
circle, PNG uint8 (see harness docstring for exact regeneration).

| Case    | Infer run 1 | Infer run 2 | Finite | Semantics | Deterministic |
| ------- | ----------- | ----------- | ------ | --------- | ------------- |
| 32×32   | 1.225 s     | 0.975 s     | yes    | relative  | yes           |
| 64×64   | 0.856 s     | 0.883 s     | yes    | relative  | yes           |
| 160×120 | 1.449 s     | 1.182 s     | yes    | relative  | yes           |
| 320×240 | 1.258 s     | 1.471 s     | yes    | relative  | yes           |

Model load ≈ 0.7–0.9 s warm (≈ 5–9 s cold in earlier sessions). Output
restored to source size in all cases; units None; `is_metric` False.

### Full pipeline (64×64)

`input_validated → preprocessing → inference_running → calibrating →
dsm_generation → mesh_generation → completed`, mesh requested and built.
Relative depth → explicit calibration → metric `height_agl_ndsm` product
(meters) → 64×64 DSM → mesh (4096 vertices / 7938 triangles). Direct
stages ≈ 0.014 s. Transport round-trip preserves relative scale,
null units, model identity, all 4096 values, and provenance.

### B. Packaging / setup

Fresh venv + `pip install -e ".[dav2]"` succeeds (depthwizard 0.1.0,
torch 2.14.0+cpu, torchvision 0.29.0+cpu, opencv-python 5.0.0); all
imports verified including provenance constants. No weight binaries
tracked (`git ls-files` clean); `checkpoints/` git-ignored.

### Core without DA-V2

Fresh venv with core install only (`numpy, Pillow, pydantic, rasterio`):
torch absent, yet `depthwizard` imports, `synthetic-depth` remains the
deterministic default, the DA-V2 module imports without torch (zero
top-level heavy imports), and 114 core tests pass.

### C. Offline / online

Zero network calls in `src/depthwizard`. Inference runs fully offline
given local checkpoint + local upstream source + installed packages.
Initial provisioning (pip packages, git clone, checkpoint download)
requires network. No offline install from an empty machine is claimed.

## Not yet established

Large-scale production performance, GPU throughput, field accuracy,
labelled-terrain benchmark accuracy, broad geographic robustness,
packaged end-user installer validation, long-duration stability.

## Conclusion

DA-V2 implementation and runtime are verified. Level-3 system evidence is
documented only to the extent actually demonstrated above. Full SIH
production readiness requires broader field/benchmark and release
evidence where still outstanding.
